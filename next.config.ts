import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Serve Inter locally via next/font/google (no external request at runtime)
  // This is the correct pattern for next/font — no manual @import needed in CSS
}

export default nextConfig
